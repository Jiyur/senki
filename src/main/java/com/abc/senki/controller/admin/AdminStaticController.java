package com.abc.senki.controller.admin;

import org.springframework.web.bind.annotation.RestController;

@RestController
public class AdminStaticController {
    //Get static per day

    //Get static per month
    //Get static per year
    //Get static per by time range
    //Get static by each product
}
